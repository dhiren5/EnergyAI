# EnergyAI Blockchain - Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# Server Configuration
PORT=3000
NODE_ENV=development

# Blockchain Configuration
MINING_DIFFICULTY=2
MINING_REWARD=100
ENERGY_TO_TOKEN_RATE=10

# Security Configuration
# Generate a strong secret key for JWT tokens (if implementing authentication)
JWT_SECRET=your-super-secret-jwt-key-change-this

# API Keys (if integrating with external services)
# Google Cloud (for Secret Manager, Vertex AI, etc.)
# NEVER put actual keys here - use Secret Manager instead!
# GOOGLE_CLOUD_PROJECT_ID=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# AWS (if using AWS services)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=use-iam-roles-instead
# AWS_SECRET_ACCESS_KEY=use-iam-roles-instead

# Azure (if using Azure services)
# AZURE_KEY_VAULT_NAME=your-key-vault-name

# Database Configuration (for future use)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=energyai_blockchain
# DB_USER=your-db-user
# DB_PASSWORD=use-secret-manager-for-this

# Monitoring & Alerts
# ALERT_EMAIL=your-email@example.com
# SLACK_WEBHOOK_URL=use-secret-manager-for-this

# Budget Limits (in USD)
MAX_DAILY_SPEND=10
MAX_MONTHLY_SPEND=100
ALERT_THRESHOLD_PERCENTAGE=80

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/blockchain.log

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit .env file to Git
# 2. Use Secret Manager for production (Google Cloud Secret Manager, AWS Secrets Manager, Azure Key Vault)
# 3. Rotate secrets regularly (every 90 days minimum)
# 4. Use short-lived credentials when possible
# 5. Enable GitHub secret scanning
# 6. Set up budget alerts in your cloud provider
# 7. Use IAM roles instead of API keys when possible
